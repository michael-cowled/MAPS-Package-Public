% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_cid_with_fallbacks.R
\name{get_cid_with_fallbacks}
\alias{get_cid_with_fallbacks}
\title{Get CID from PubChem with Fallbacks and Caching}
\usage{
get_cid_with_fallbacks(name, smiles = NA, db_con = NULL)
}
\arguments{
\item{name}{A character string representing the compound name.}

\item{smiles}{A character string representing the compound SMILES (optional).}
}
\value{
A named list with keys: \code{CID}, \code{ResolvedName}, \code{SMILES},
or \code{NULL} if all lookups fail. MolecularFormula and MonoisotopicMass are no longer returned.
}
\description{
Attempts to resolve a PubChem CID using compound name, SMILES, and synonyms.
If successful, returns a list of annotation details, and updates a global cache.
}
\details{
This function expects a global object named \code{cid_cache_df} to exist,
and it will be updated via \code{<<-} with new entries when successful lookups are made.
}
\examples{
\dontrun{
# Assuming cid_cache_df is initialized globally as per your setup
get_cid_with_fallbacks("glucose")
get_cid_with_fallbacks("some compound", smiles = "CCO")
}
}
